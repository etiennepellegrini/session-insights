{
  "description": "Session insights hooks: curation nudge on SessionStart",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/check-insights-size.bash",
            "timeout": 3
          }
        ]
      }
    ]
  }
}
